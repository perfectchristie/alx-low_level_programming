#include "main.h"
#include <stdio.h>

/**
 * swap_int -> swaps the values of two integers using two input parameters
 * @a: input parameter 1
 * @b: input parameter 2
 * Return: success
 */

void swap_int(int *a, int *b)
{
	int tmp;

	*a = tmp;
	*a = *b;
	*b = tmp;
}
