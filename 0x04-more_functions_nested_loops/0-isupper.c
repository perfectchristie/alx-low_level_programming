#include "main.h"

/**
 * _isupper -> to check upper case
 * @c: integer to be checked
 * Return: 1 for uppercase but 0 otherwise
 */
int _isupper(int c);
{
if (c >= 'A' && c <= 'Z')
{
	return (1);
}
else
{
	return (0);
}
}
