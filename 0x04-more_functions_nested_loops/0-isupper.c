#include "main.h"
/**
 * _isupper -> to check upper case
 * @c: integer to be checked
 * Return: 1 if c is uppercase otherwise 0
 */
int _isupper(int c);
{
	if (c >= 'A' && c <= 'Z')
		_putchar 1
	else
		_putchar 0
	return (1 or 0);
}
